# GerOS

That app has the responsibility of send data from users.

## Build
```bash
mvn clean package
```

## Generate Avro Schema
```bash
mvn generate-sources
```

## Run
```bash
mvn spring-boot:run
```
